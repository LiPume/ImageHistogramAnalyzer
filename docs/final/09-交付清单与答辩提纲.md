# 交付清单与答辩提纲

## 一、最终交付信息

| 项目 | 内容 |
|---|---|
| 当前提交候选 versionName/versionCode | `3.0 / 3` |
| 已验证 App 代码基线 | `d1b207e` |
| Git tag | `[待填写]` |
| 当前 Debug APK | `app-debug.apk`，13,053,534 bytes |
| 当前 Debug APK SHA-256 | `6ea9c143573ad97b47619c7d9f6359c659f63afb0e912807b8d9885c4cd9730c` |
| 测试设备/系统 | Xiaomi 14 / Android 16 / HyperOS 3.0.303.0.WNCCNXM.C09 |
| 提交日期 | `[待填写]` |
| 课堂验收 PPT | `ImageHistogramAnalyzer-课堂验收汇报.pptx`，11 页，5–8 分钟 |
| PPT 文件大小 / SHA-256 | 2,815,257 bytes / `99dc0b438fe18c9655cf1c6d939f9994529ec3ac589720e384655b0a0de8ff09` |

## 二、源码与构建检查

- [x] `main` 在本轮开始时已与远程同步；最终封版前需再确认；
- [x] `./gradlew :app:testDebugUnitTest :app:assembleDebug :app:assembleDebugAndroidTest :app:lintDebug` 已于 2026-07-08 通过；
- [x] API 36.1 AVD 连接测试通过，性能 benchmark 按设计跳过；
- [ ] Release/最终 APK 可安装、启动、选图和计算；
- [x] Git 忽略 `build/`、`.gradle/`、`local.properties`、签名文件和第三方参考仓库；
- [x] `test/`、`test_pic/` 中的私人图片和截图不作为独立文件提交；
- [x] 当前受控文档和源码未写入 token、账号、证书或签名密码。

## 三、文档检查

- [ ] 本目录九份文档中的 `[待填写]` 已处理；
- [x] 需求、架构、接口、算法、测试和使用说明已按当前代码统一；
- [x] 灰度公式、取整、256-bin、0–100 归一化和 300ms 口径一致；
- [ ] 真机表覆盖全部 17 个图片 ID，并包含 SHA-256、构建类型、commit、电量和两个方案各一次的分段数据；
- [ ] L1 两方案核心总计与最高频次已填写，B02 超限拒绝已记录；
- [x] v2/v3 非严格同图的历史数据只作为方向证据；
- [x] 开源项目只记录技术借鉴，没有复制第三方代码。
- [x] 课堂 PPT 已逐页渲染并通过溢出检查；最终 L1 占位符等待真机表回填。

## 四、建议截图

1. 首页和“选择图片”；
2. 系统选择器或拍照入口；
3. 图片预览、分辨率和像素数；
4. 两种方案选择卡；
5. 256×100 白底黑柱直方图；
6. 质量分析分布条和结论；
7. 两种方案的 Native v3 性能卡；
8. 300ms 达标条；
9. 错误提示和重新选择；
10. 自动化测试 BUILD SUCCESSFUL。

截图前隐藏通知、联系人、账号、文件绝对路径等个人信息。截图说明应包含版本、设备、图片尺寸和方案。

## 五、答辩演示流程（约 5–8 分钟）

1. 介绍课题目标：移动端生成 256×100 灰度直方图，核心计算目标 300ms；
2. 展示首页和本地处理边界；
3. 选择 12.58MP 图片，说明选图后不会自动计算；
4. 运行“优先灰度化”，展示直方图、质量和分段耗时；
5. 切换“统计时灰度化”，比较融合阶段和核心耗时；
6. 解释 v1→v2→v3：热循环优化仍受 `getPixels` 限制，Native 直读消除大数组复制；
7. 展示自动化一致性证据和完整的 17 行真机表；
8. 说明限制：1600 万像素上限、相机入口依赖系统、质量分类为规则提示；
9. 总结结果与自研边界。

课堂 PPT 使用项目专用 [course-presentation Skill](../../.github/skills/course-presentation/SKILL.md) 生成，视觉采用固定 light、瑞士网格和项目蓝强调色。最终 L1 数值未填写前，PPT 必须保留 `[待真机填写]`，不得把历史杯子图数据冒充固定 L1。

## 六、常见答辩问题

### 为什么不直接使用 `getPixel()`？

逐像素 Java/Native 边界调用开销高。v2 使用批量 `getPixels`，v3 进一步使用 `AndroidBitmap_lockPixels` 直接读取 Bitmap。

### 两种方案有什么区别？

优先灰度化先保存完整灰度缓冲再统计，结构直观但多一次扫描和缓冲；统计时灰度化把转换和计数融合，减少内存和遍历。

### 如何保证优化没有算错？

Kotlin/Native、两方案都使用同一规则；测试覆盖全部 RGB、`.5` 取整边界、透明像素和逐 bin 一致性。结果不一致时不允许报告加速。

### 为什么 300ms 不包含解码和 Canvas？

课程目标对应直方图核心生成。项目仍单独展示解码，Canvas 只消费 256 个归一化高度，避免把不同阶段混成一个不可解释数字。

### 为什么没有继续做 NEON/OpenCV？

Native v3 已有充足性能余量。继续引入显式 SIMD 或大型库会增加复杂度和转换成本，缺少验收收益；若最终 L1 实测重新超过 300ms 才重新评估。

### 图像质量判断是否专业？

不是。它是基于平均灰度、暗亮占比和标准差的课程级规则提示，界面和文档均不宣称医学或专业摄影诊断。

## 七、最终结论模板

> 本项目在 Xiaomi 14、Android 16 / HyperOS 3.0.303.0.WNCCNXM.C09 上完成图片选择、两种灰度直方图流程、256×100 绘制、质量分析和分段性能展示。最终真机表覆盖 17 个图片 ID；对 SHA-256 为 `783198e25198cb7fabb18db9f690224bb4402fcb27737f46c81c1b11a2f5daec` 的 12.58MP 固定 L1，优先灰度化与统计时灰度化核心耗时分别为 `[待真机填写]ms / [待真机填写]ms`，两方案最高频次均为 `[待真机填写]`，最终 300ms 结论为 `[待真机填写]`。Kotlin/Native 与两种流程的 256-bin 一致性自动化测试通过。当前 App 代码基线为 `d1b207e`；当前 Debug APK SHA-256 为 `6ea9c143573ad97b47619c7d9f6359c659f63afb0e912807b8d9885c4cd9730c`。
