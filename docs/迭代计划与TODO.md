# 迭代计划与 TODO

> 状态说明：`未开始`、`进行中`、`待验收`、`已完成`、`已搁置`、`阻塞`。
> 更新规则：每次重要代码变更后更新对应 TODO；阶段结束前逐项核对验收标准。

## TODO 使用规则

- 任务编号保持稳定，提交、测试报告和会议记录均引用编号，例如 `MVP-04`。
- 每个任务开工前填写负责人；完成后勾选并补充对应提交或测试记录。
- `P0` 为阶段主链路，优先完成；`P1` 为本阶段必要完善项；`P2` 为有余力再做的增强项。
- 依赖未完成时不并行修改高冲突文件；需要调整范围时先更新本文件。
- 任务完成定义：代码/文档产物存在、相关检查通过、TODO 状态同步，不以“代码写完”单独判定完成。

## 阶段 0：项目治理初始化

- 当前状态：**已完成**
- 阶段目标：建立 Git 仓库、参考资料目录和文档目录，明确团队协作规范。
- 负责人：`lzx`

### 开发任务与具体 TODO

- [x] `GOV-01`（P0）检查 Git 状态，初始化本地仓库并将默认分支设为 `main`。
- [x] `GOV-02`（P0，依赖：GOV-01）配置 `origin` 为 `git@github.com:LiPume/ImageHistogramAnalyzer.git`。
- [x] `GOV-03`（P0）建立 `references/open_source/`，拉取两个参考仓库并由 `.gitignore` 排除。
- [x] `GOV-04`（P0，依赖：GOV-03）建立并验证 `scripts/fetch_references.sh`。
- [x] `GOV-05`（P0）完善 `.gitignore`，排除构建、本机、原始 Word 和第三方参考内容。
- [x] `GOV-06`（P1）建立 README、开源借鉴记录、迭代计划和协作规范。
- [x] `GOV-07`（P0，依赖：GOV-05）确认首次提交范围，`docs` 只上传 Markdown。
- [x] `GOV-08`（P0，依赖：GOV-01~07）完成阶段性 commit 并推送 `main`。
- [x] `GOV-09`（P1）填写团队成员和模块负责人。
- [x] `GOV-10`（P1）评估 Android Agent Skills 素材库并建立项目专用 skill 与代理入口。
- [x] `GOV-11`（P1）建立 Git 检查点、文档同步和 Android 测试门禁三个配套 skill。

### 预期产物

- 本地 Git 仓库、`main` 分支和正确的远程地址。
- 可复用的参考仓库拉取脚本。
- README、协作规范、开源借鉴记录和迭代 TODO。
- 不包含第三方仓库、构建目录和本机配置的首次提交。

### 验收标准

- `git status` 中不出现 `build/`、`.gradle/`、`local.properties`、`.idea/` 或 `references/open_source/`。
- 新组员执行脚本即可获得两个参考仓库。
- 全组能够说明分支、提交、文档同步和冲突规避规则。
- GitHub 的 `main` 分支包含课程项目自身文件，但不包含第三方源码。

## 阶段 1：MVP 基本功能

- 当前状态：**已完成**。MVP 初版的 12,582,912 像素核心计算曾超过 300ms；该问题已在阶段 3 的 Native v3.0 中解决。
- 阶段目标：App 能启动并在手机运行，完成选图、预览、基础直方图和耗时展示。
- 负责人：`lzx`

### 开发任务与具体 TODO

- [x] `MVP-00`（P0）建立 `data/domain/ui` 与测试目录骨架，不加入业务实现。
- [x] `MVP-01`（P0，依赖：GOV-08；目标文件：Gradle 配置）配置 Kotlin + Compose、Material 3、Lifecycle 和测试依赖。
- [x] `MVP-02`（P0，依赖：MVP-01；目标文件：`MainActivity.kt`、Manifest、theme）建立 Launcher Activity，并完成 API 36 模拟器与 lzx 实体手机启动验证。
- [x] `MVP-03`（P0，依赖：MVP-02；模块：`data/image`）接入系统相册选择器和 URI 状态，不申请广泛存储权限。
- [x] `MVP-04`（P0，依赖：MVP-03；模块：`data/image`、`ui/component`）实现受控解码和图片预览。
- [x] `MVP-05`（P1，依赖：MVP-04）显示图片分辨率、像素数量和格式/错误信息。
- [x] `MVP-06`（P0，负责人：`lzx`；模块：`domain/model`）定义与 UI 解耦的直方图输入/输出数据模型。
- [x] `MVP-07`（P0，依赖：MVP-06；模块：`domain/histogram`）明确灰度取整、Alpha 和异常输入规则，并先建立确定性测试。
- [x] `MVP-08`（P0，依赖：MVP-07）按指定公式实现基础灰度频次统计。
- [x] `MVP-09`（P0，依赖：MVP-08）将 256 个频次归一化到 0..100，并覆盖除零/单峰测试。
- [x] `MVP-10`（P0，依赖：MVP-09；模块：`ui/component`）使用 Canvas 绘制 256×100 逻辑直方图。
- [x] `MVP-11`（P0，依赖：MVP-04、08、09；模块：`ui/analyzer`）由 ViewModel 串联选图、后台计算和 UI State。
- [x] `MVP-12`（P0，依赖：MVP-11）显示核心计算耗时及 300ms 判断，并标明计时边界。
- [x] `MVP-13`（P1，依赖：MVP-02~12）完成真机主流程测试、MVP 截图和文档同步；真机性能未达标项转入 `PERF-04` 及后续优化任务。
- [x] `MVP-14`（P0，依赖：MVP-10）使用整页 `LazyColumn` 承载预览、方案、直方图和性能卡片，并用 Compose 测试验证屏幕外内容可滚动到达。
- [x] `MVP-15`（P0，依赖：MVP-04、11）将交互调整为“选择图片 → 选择方案 → 手动计算”，选图后只解码和预览，不自动生成直方图。
- [x] `MVP-16`（P0，依赖：MVP-08、09、15）独立实现“优先灰度化”和“统计时灰度化”两种课程方案，共用灰度取整规则和归一化模块，并逐 bin 验证结果一致。
- [x] `MVP-17`（P1，依赖：MVP-14~16）建立本地真实图片回归集和安装脚本，完成三张 JPG 的 API 36 模拟器主流程测试；图片目录由 Git 忽略。
- [x] `MVP-18`（P1，依赖：MVP-17）新增两张 PNG 日间/夜景测试图，将本地回归集扩充为五张；已通过脚本安装到 API 36 模拟器并核对 MediaStore 文件名、格式和尺寸，图片继续由 Git 忽略。

### 预期产物

- 可启动、可安装的 MVP Debug APK。
- 图片选择、预览、方案选择、手动计算、绘制和耗时展示完整链路。
- 基础算法说明、核心数据模型和 MVP 演示截图。

### 验收标准

- 真机能够启动、选择图片并稳定显示预览，选图后不会自动计算。
- 两个方案都可独立选择和执行，256 个 bin 的结果保持一致。
- 界面显示分辨率、像素总数、256×100 直方图和计算耗时。
- 页面可滚动到直方图和性能卡片，不因屏幕高度而遮挡结果。
- 核心算法不写在 `MainActivity.kt` 或 Canvas 绘制回调中。
- 选择和计算期间界面无明显假死，失败状态有中文提示。

## 阶段 2：基础功能完善

- 当前状态：**已完成（2026-07-08）**。v4.0 真机页面与性能截图已复核，错误恢复、状态 UI 和确定性质量夹具已由自动化测试覆盖。
- 阶段目标：完善质量分析、错误处理、测试用例、截图和使用说明。
- 负责人：`lzx`

### 开发任务与具体 TODO

- [x] `BASE-01`（P0，依赖：MVP-09）基于 256 个频次计算加权平均灰度，不重新扫描 Bitmap。
- [x] `BASE-02`（P0，依赖：MVP-09）计算暗部 `0..63` 占比、亮部 `192..255` 占比和总体灰度标准差。
- [x] `BASE-03`（P0，依赖：BASE-01、02）集中定义质量阈值：平均灰度 `<85` 或暗部占比 `≥60%` 为偏暗；平均灰度 `>170` 或亮部占比 `≥60%` 为偏亮；其余图像标准差 `<35` 为低对比度，否则正常。2026-07-09 根据实时遮挡镜头反馈补充“左半灰度主导且均值偏低/右半灰度主导且均值偏高”的判定，避免直方图明显集中在黑色侧或白色侧时误判为正常。判定优先级为偏暗、偏亮、低对比度、正常。
- [x] `BASE-04`（P1，依赖：MVP-03~05）已覆盖取消选择、损坏图片、超大图片和无法打开；图片加载边界可注入测试，错误提示按类型使用中文说明，替换失败时保留上一份成功图片与元数据。
- [x] `BASE-05`（P0，依赖：BASE-01~03）新增 8 项质量分析单元测试，覆盖纯黑、纯白、偏暗、偏亮、中灰低对比度、宽分布正常、暗部主导和频次总和异常。
- [x] `BASE-06`（P1，依赖：MVP-13）已覆盖质量卡片滚动、加载状态、错误状态、重新选图动作、选择器取消和替换失败保留旧内容；Compose/设备测试以用户可见文本、按钮状态和回调为断言。
- [x] `BASE-07`（P1）已用确定性 256-bin 夹具覆盖纯黑、纯白、偏暗（灰度 80）、偏亮（灰度 180）、中灰低对比度和宽分布正常，并记录精确期望值；五张本地真实图继续用于视觉与真机回归，不把主观照片分类当成算法真值。
- [x] `BASE-08`（P1，依赖：BASE-04~07）已完成 v4.0 功能/性能截图复核，并同步使用说明、测试报告和性能数据表初稿。
- [x] `BASE-09`（P1，依赖：MVP-11）现场拍照能力已由系统 `PickVisualMedia` 选择器提供，lzx 在 Xiaomi 真机确认可直接拍照并返回现有 URI 分析链路。原定额外 `FileProvider`、相机权限和独立拍照按钮被此系统方案替代，避免重复入口与权限维护；是否显示相机入口由设备系统选择器决定。

### 预期产物

- 功能完整的基础版本 APK。
- 图像质量指标与判断结果页面。
- 基础测试用例、测试图片、截图和使用说明初稿。

### 验收标准

- 各质量指标可由直方图复算，数值范围和判断结果正确。
- 黑图、白图、暗图、亮图和低对比度图通过确定性测试。
- 常见错误不会导致崩溃、ANR 或永久加载。
- 文档、界面术语和代码实现保持一致。

## 阶段 2.5：UI 规范与体验优化

- 当前状态：**已完成（2026-07-07）**
- 阶段目标：先研究公开 Android Agent Skills 并沉淀项目规则，再以 Material 3、Compose、可访问性和可测试性为依据优化首页、分析流程、返回路径与结果可视化。
- 负责人：`lzx`

### 开发任务与具体 TODO

- [x] `UI-01`（P0）已只读研究 7 个公开仓库的 README、主 Skill 与 Compose/Material 3/性能/测试/无障碍相关文件；未执行第三方脚本、未安装依赖，来源和采用边界见 `docs/agent-skill-study.md`。
- [x] `UI-02`（P0）已确认项目为单 `:app` 模块、Kotlin + Compose + Material 3 + ViewModel/StateFlow，无 XML 页面、Navigation、Room、Hilt/Koin、Retrofit；修改前 `:app:assembleDebug` 通过，Git 仅有本阶段 TODO 与未跟踪 `test/`。
- [x] `UI-03`（P0，依赖：UI-01、02）已更新根目录 `AGENTS.md`，创建 `docs/agent-skill-study.md` 和项目本地 `skills/android-ui-material3/SKILL.md`，明确单模块和课程边界。
- [x] `UI-04`（P0，依赖：UI-01、02）已创建 `docs/ui-design-system.md`，定义主题色彩、排版、间距、圆角、组件、页面状态、信息层级、图表和无障碍规范；当前主题决策由 `UI-11` 更新为固定 light。
- [x] `UI-05`（P0，依赖：UI-04）首屏和分析页选图按钮均改为真实动作“选择图片/重新选择图片”，不再把打开系统选择器表述为“开始分析”。
- [x] `UI-06`（P0，依赖：UI-04）新增可滚动首页，说明本地选图/系统选择器拍照、两种计算流程、质量判断、性能分段和 300ms 口径。
- [x] `UI-07`（P0，依赖：UI-04）新增首页/分析页轻量目的地状态、分析页“返回首页”和系统返回处理；回首页后保留已选图并提供“继续查看分析”，未新增 Navigation 依赖。
- [x] `UI-08`（P1，依赖：UI-04）质量卡增加暗部/中间调/亮部分布条，性能卡增加 300ms 预算条；保留全部精确数值、文字结论和计时口径，未新增图表依赖。
- [x] `UI-09`（P1，依赖：UI-05~08）补齐 ColorScheme、Typography、Shapes 和间距 token；后续 `UI-11` 按用户选择固定 light，主要按钮由 M3 保证 48dp，页面标题和图表补齐语义。
- [x] `UI-10`（P0，依赖：UI-05~09）新增首页动作/能力说明/继续分析、返回首页和两条图表语义测试；`testDebugUnitTest`、`assembleDebug`、`assembleDebugAndroidTest`、`lintDebug` 均通过，API 36.1 模拟器 12 项通过、1 项显式 benchmark 按设计跳过、0 失败。
- [x] `UI-11`（P1）已将用户选定的 B“晴空柑橘”固定 light 方案落地：蓝色主按钮，浅蓝/薄荷绿/淡柑橘黄功能模块，蓝/橙/绿质量分布和蓝色性能预算；补齐蓝灰 surface containers，算法选中态统一浅蓝，消除紫色底卡与绿色选中态冲突；课程直方图继续保持白底黑柱。API 36.1 模拟器在系统 dark mode 下仍显示固定 light，实际选图后验证方案卡未出现紫/绿冲突；JVM/构建/Lint 通过，设备测试 12 项通过、1 项显式 benchmark 跳过、0 失败。

### 本阶段产物

- 项目级 Agent/UI 规则：`AGENTS.md`、`skills/android-ui-material3/SKILL.md`。
- 研究与设计文档：`docs/agent-skill-study.md`、`docs/ui-design-system.md`。
- 新首页、可返回分析流程、语义一致的选图按钮、质量分布条和 300ms 预算条。
- 固定 light、1.5× 字体和系统 dark mode 下仍保持 light 的模拟器检查、Compose UI 回归和完整 Debug 构建门禁记录。

### 验收标准

- 首页主按钮文案与实际“选择图片”动作一致，首页有简洁功能说明且不存在无意义空白。
- 用户进入分析流程后可明确返回首页，返回不破坏已验证的图片、直方图、质量分析和性能功能。
- 图表只表达重要比例或阶段关系，精确数值仍可读；不新增大型 UI/图表依赖。
- UI 逻辑保持状态提升，Composable 不执行图片解码、直方图或质量算法。
- 固定 light、窄屏滚动、字体缩放、触控目标、语义和主要状态通过检查与自动化测试。

## 阶段 3：性能优化与验收

- 当前状态：**性能开发暂停，最终全 ID 验收待办**。v3.0 已在 Xiaomi 14 单次测试中以较大余量达到 300ms；不再继续 NEON、第三方库和低收益微调，课程验收前执行 `PERF-06/07/10`。
- 阶段目标：正式比较“优先灰度化”与“统计时灰度化”，完成 300ms 真机验证和课程验收材料。
- 负责人：`lzx`

### 首轮真机基线（2026-07-06）

- 图片：`1783344131211.jpg`，3072×4096，共 12,582,912 像素。
- 图片解码：102.836ms。
- 优先灰度化：核心计算 1415.321ms，约为 300ms 目标的 4.72 倍。
- 统计时灰度化：核心计算 1304.234ms，约为 300ms 目标的 4.35 倍。
- 测试设备：Xiaomi 14，第三代骁龙 8 移动平台，八核最高 3.3GHz，16GB RAM，Android 16，OS 版本 `3.0.303.0.WNCCNXM.C09`。
- 两方案最高灰度频次均为 121,520；当前为单次部署构建数据，Debug/Release 类型仍待确认。

### v2.0 真机优化结果（2026-07-07）

本轮使用同一杯子场景，截图中的分辨率、像素数和最高频次均与首轮一致；v2.0 文件名显示为 `1783403529271.jpg`。正式报告仍需固定原始测试文件并记录校验值。

| 阶段 | 优先灰度化 | 统计时灰度化 |
|---|---:|---:|
| 图片解码（核心外） | 113.250ms | 113.250ms |
| Bitmap 批量取像素 | 539.234ms | 499.510ms |
| 灰度转换 | 195.738ms | 与统计融合 |
| 频次统计 | 40.181ms | 173.939ms（融合） |
| 归一化 | 0.251ms | 0.225ms |
| 其他开销 | 0.180ms | 0.060ms |
| 多线程合并 | 未启用 | 未启用 |
| 核心总耗时 | 775.583ms | 673.735ms |

结果判断：

- 相比 v1.0 的 1415.321ms / 1304.234ms，两个方案分别加速约 1.82 倍和 1.94 倍，核心耗时下降 45.2% 和 48.3%，说明 `PERF-11` 有效。
- 两方案最高灰度频次仍为 121,520，截图层面未发现结果漂移；完整正确性继续由 256-bin 与全 RGB 自动化测试保证。
- `Bitmap.getPixels()` 占核心时间约 69.5% / 74.1%，且单独已超过 300ms，是当前首要瓶颈。只并行灰度统计无法让总时间达标。
- 优先灰度化的灰度转换与统计合计约 235.919ms；统计时灰度化的融合阶段约 173.939ms，当前算法计算本身已明显低于像素复制耗时。
- 同图两次 `getPixels()` 相差约 39.7ms，当前仍是单次 Debug/构建类型待确认数据，需要预热和多轮中位数避免偶然波动。
- 12.58MP ARGB_8888 Bitmap、彩色 `IntArray` 各约 48MiB；优先灰度化再增加约 48MiB 灰度 `IntArray`，三者理论驻留约 144MiB，存在 GC 和峰值内存风险。

更新后的优化顺序：先建立可复现多轮基准，再比较像素访问路径并消除 Java 整图复制；之后才并行灰度统计。若 Android/Kotlin 路径仍不能把取像素降到预算内，则使用 NDK `AndroidBitmap_lockPixels` 直接访问 Bitmap，并进一步评估 ARM NEON。所有方案必须包含转换/拷贝成本并通过 256-bin 完全一致性测试。

建议性能预算：像素访问 ≤100ms，灰度转换与统计 ≤150ms，归一化/合并/其他 ≤10ms，核心中位数 ≤260ms，为 300ms 验收线保留约 40ms 波动空间。

### v3.0 本地优化结果（2026-07-07）

本轮不是单点微调，而是替换核心执行引擎：通过 NDK `AndroidBitmap_lockPixels` 直接访问 RGBA_8888 Bitmap，在 C++ 中完成两种课程流程；按行分块、线程私有 256-bin 后归并，避免共享计数竞争。优先灰度化的中间灰度由 4-byte `IntArray` 改为 1-byte native 缓冲；Native 不可用时仍回退 Kotlin v2。界面新增执行引擎、工作线程和 Native 分段耗时。

API 36/arm64 模拟器先全局 JIT 预热 20 次，再按尺寸预热 5 次并正式运行 10 次。12,582,912 像素确定性夹具结果：

| 方案 | Kotlin v2 中位/P90 | Native v3 中位/P90 | 中位加速 |
|---|---:|---:|---:|
| 优先灰度化 | 95.948 / 121.883ms | 30.885 / 32.451ms | 3.11× |
| 统计时灰度化 | 64.455 / 69.353ms | 15.067 / 16.305ms | 4.28× |

该 AVD 的 `nproc=1`，所以 1/2/4/6/8 线程没有稳定缩放收益；这些数字证明的是 Native 直读、内存压缩和 C++ 热循环有效，不用于宣称八核并行收益。真机表、完整模拟器明细和后续版本占位见 `docs/性能优化数据表.md`。

正确性方面，Kotlin 快路径已穷举全部 16,777,216 种 RGB；Native 又覆盖全部 16,782 个精确 `.5` 取整边界、混合 Alpha Bitmap、两种方案的 256-bin 与 0–100 归一化一致性。当前未写显式 ARM NEON，也不把 `-O3` 可能产生的自动向量化宣传为已实现 SIMD。

### v3.0 真机达标结果（2026-07-07）

- 设备：Xiaomi 14，8 个 Native 工作线程。
- 图片：`1783408658743.jpg`，3072×4096，共 12,582,912 像素；两方案最高灰度频次均为 124,455。
- 优先灰度化：解码 117.465ms（核心外），核心计算 56.957ms。
- 统计时灰度化：解码 117.465ms（核心外），核心计算 22.244ms。
- 两方案分别只占 300ms 预算约 19.0% 和 7.4%，单次结果均有充足余量。

决策：v3.0 已满足“核心计算 300ms 内”目标，性能方向进入冻结状态。v2 与 v3 图片的最高频次不同（121,520 与 124,455），因此约 13.62× / 30.29× 的对比仅作为同尺寸、相似场景参考，不冒充严格同文件加速比。2026-07-08 起最终课程验收改为“一张图片 ID 一行、两方案各一次”；历史多轮模拟器数据继续作为稳定性旁证。

### 开发任务与具体 TODO

- [x] `PERF-01`（P0，依赖：MVP-08、09）固化“优先灰度化”方案：先生成逐像素灰度数组，再统计并归一化。
- [x] `PERF-02`（P0，依赖：PERF-01）固化“统计时灰度化”方案：批量读取彩色像素后在统计循环中完成灰度化，避免中间灰度数组。
- [x] `PERF-03`（P0，依赖：PERF-02）用确定性测试逐 bin 验证两种方案输出完全一致。
- [x] `PERF-04`（P0，依赖：PERF-03）建立低干扰分阶段计时模型：记录 `Bitmap.getPixels()`、灰度转换、256-bin 累计、归一化、多线程合并、其他分配/调度开销和核心总耗时；解码单独记录，Canvas/首帧由 `PERF-17` 独立验证。使用单调纳秒时钟，只在阶段边界采样，不在逐像素循环中打点。
- [x] `PERF-05`（P1，依赖：PERF-04）扩展性能卡片：优先灰度化分别显示转换和统计，统计时灰度化显示融合阶段；同时显示取像素、归一化、合并状态、其他开销、核心总耗时和 300ms 结论。
- [ ] `PERF-06`（P0，依赖：PERF-16）在 Xiaomi 14 的最终构建上对固定 L1 运行两个方案各一次，填写完整分段、最高频次、电量和 300ms 结论。
- [ ] `PERF-07`（P1，依赖：PERF-16）完成 S01–S08、L1、B01/B02、T1–T5、W01 共 17 个 ID 的单行真机表；除 B02 外两个方案各运行一次，记录一致性、质量和异常。
- [ ] `PERF-08`（P0，状态：**已搁置**）原计划用 Profiler 深挖 Kotlin `Bitmap.getPixels()`；Native v3 已取消该复制且像素锁定仅约 0.002–0.003ms，不再分析已退出主路径的旧瓶颈。
- [x] `PERF-09`（P0，依赖：PERF-05、16、20）已形成中文性能测试表，包含模拟器多尺寸/线程矩阵、真机 v1/v2/v3 历史记录，以及最终 17 个 ID 的单行填写表。
- [ ] `PERF-10`（P0，依赖：全部阶段任务）整理 APK、源码、测试报告、使用说明和答辩材料。
- [x] `PERF-11`（P0，依赖：PERF-04）优化 Kotlin 单线程热循环：用分块索引 `while` 循环替代高阶遍历，移除逐像素取消取模；灰度转换采用整数定点快路径，仅在精确 `.5` 边界回退原浮点公式。已穷举 256³ 种 RGB，和旧实现零差异；API 36 模拟器同图单次核心耗时由 178.310ms 降至 101.525ms（仅开发信号）。
- [ ] `PERF-12`（P0，状态：**已搁置**）原计划比较复用数组、分块读取和直接缓冲区；v3 已通过 `AndroidBitmap_lockPixels` 完全绕过 Java 整图复制并真机达标，不再投入低收益 Kotlin 像素访问矩阵。
- [ ] `PERF-13`（P0，状态：**已搁置其余调优**）多线程主体已实现：按行分块、线程私有 256-bin、归并和 1/2/4/6/8 线程模拟器矩阵；Xiaomi 14 使用 8 线程已达标。暂不继续线程数真机调参和 Native 中途取消，旧请求结果已有请求编号保护。
- [x] `PERF-14`（P0，依赖：PERF-12）完成普通 C++/JNI v3.0：使用 `AndroidBitmap_lockPixels` 直接读取软件 Bitmap，避免 Java 彩色 `IntArray` 整图复制，在 native 层完成局部 256-bin 与归并；保留 Kotlin 回退并记录锁像素、灰度、统计、合并、归一化和线程数。显式 NEON 已拆为有证据才执行的 `PERF-19`。
- [ ] `PERF-15`（P2，状态：**已搁置**）课程允许 OpenCV 等第三方实现，但当前 Native 自研路径已有充足性能余量；不为继续追求无验收价值的毫秒数增加 Bitmap/Mat 转换、APK 体积和维护成本。
- [x] `PERF-16`（P0，依赖：PERF-03、11、14、20）确定 Native v3 为最终优化路径；自动化测试已验证 Kotlin/Native、两流程、取整边界、Alpha、256-bin 和归一化完全一致，真机截图中两方案最高频次同为 124,455。
- [ ] `PERF-17`（P1，依赖：PERF-05）使用帧时间或 Macrobenchmark 类工具独立记录结果提交到直方图首帧呈现的耗时；不使用 Canvas 回调内更新状态的方式干扰绘制，也不把渲染时间混入 300ms 核心计算。
- [x] `PERF-18`（P0，状态：**以历史模拟器基准完成并由最终口径替代**）优化开发期已固定夹具并形成预热、多轮、中位数/P90数据；最终课程表不再重复该实验，改为 17 个 ID 各一次，只记录电量，不记录温度。单次真机表仅用于本次验收，不宣称长期稳定性。
- [ ] `PERF-19`（P1，状态：**已搁置**）ARM NEON 的触发条件是 v3 真机不能达标；当前单次核心仅 56.957ms / 22.244ms，暂无必要。若最终 L1 实测重新超过 300ms 再启用。
- [x] `PERF-20`（P0，依赖：PERF-14）完成 v3.0 本地交付门禁：版本号升级为 3.0；四 ABI 构建；Native/Kotlin 与两课程方案一致性测试；性能卡片显示引擎和线程；中文模拟器 CSV、真机版本表和一键基准脚本已生成。

### 预期产物

- “优先灰度化”与“统计时灰度化”两套独立算法。
- 算法一致性测试、性能对比页面和性能测试表。
- 验收版 APK、完整源码、测试报告与答辩材料。

### 验收标准

- 两种算法输出完全一致，优化不能以牺牲正确性为代价。
- 性能卡片能够解释总耗时主要消耗在哪个中间阶段，分段计时不进入逐像素热循环。
- 指定测试设备和图片上，核心指标稳定满足 300ms 要求；未达标时报告真实结果和原因。
- 连续处理多张图片无明显内存持续增长、OOM 或 ANR。
- 验收材料能够说明灰度公式、归一化、计时边界、优化方法和实测数据。

## 阶段 4：最终文档与交付整理

- 当前状态：**进行中（2026-07-10）**。最终文档主体、17 个 ID 真机操作步骤、一行一 ID 的 Excel/CSV、v5.0 截图资产和课堂 PPT 本地更新已建立；等待 lzx 填写真机数据、最终 APK 与提交日期后封版。
- 阶段目标：把过程材料收敛为可提交、可复现、可答辩的最终文档集。
- 负责人：`lzx`

### 开发任务与具体 TODO

- [x] `DOC-01`（P0）建立 `docs/final/` 最终文档总入口，明确过程文档、最终文档和课程原始材料的关系。
- [x] `DOC-02`（P0）建立项目概述、需求、架构、接口、算法性能、测试用例、真机记录、部署使用和交付答辩九份最终文档框架。
- [x] `DOC-03`（P0）建立 17 个图片 ID 的 Excel/CSV 真机模板；每个 ID 一行、两方案并列，已预填文件、尺寸、像素数、SHA-256、预期、版本、设备、引擎与线程，自动判断频次一致和 300ms 结论；保留电量并删除温度。
- [ ] `DOC-04`（P0，依赖：PERF-06、07，状态：**部分完成**）最终文档中可由代码、构建和既有证据确定的内容已填写；仍需 lzx 在 Xiaomi 14 补齐 17 行实测、截图、最终构建 commit/APK 与提交日期。
- [ ] `DOC-05`（P1）补充最终首页、选图、两方案直方图、质量卡片、性能卡片和错误状态截图，并移除或遮挡个人信息。
- [ ] `DOC-06`（P0，依赖：PERF-10）填写最终版本号、Git commit/tag、APK 文件名、SHA-256、安装验证结果和课程提交日期，完成文档封版。
- [x] `DOC-07`（P1）建立 `test/final/` 可复现测试集：11 张确定性 PNG、5 张本地真实图和 1 张 Google 官方 WebP；补齐生成、下载、安装脚本以及尺寸、SHA-256、来源和期望结果清单。图片目录保持 Git 忽略。
- [x] `DOC-08`（P0）建立 `.github/skills/course-presentation/` 项目专用课堂汇报 Skill，并生成早期课堂验收 `docs/final/ImageHistogramAnalyzer-课堂验收汇报.pptx`；该 12 页稿当时按要求不纳入 Git，后续已由第十四组正式 15 页终版替代，二进制提交策略由 `DOC-14` 覆盖。
- [x] `DOC-09`（P0）阅读 `docs/功能迭代计划.md`，将增强功能拆分到阶段 5 的可执行 TODO；明确先做离线静态分析增强，再做 ROI，最后做 CameraX 智能拍摄助手，避免一次性引入过多风险。
- [x] `DOC-10`（P1）整理 CameraX 智能拍摄助手的真机迭代问题，形成“实时相机直方图”和“自动优化与手动微调”两套答辩话术，并补充精简版与常见追问回答。
- [x] `DOC-11`（P0）以小组文字稿为母稿，使用 `test/v5.0/` 与答辩截图生成图文美化过程稿；保留可编辑的产品流程、架构、ROI 和拍摄优化闭环图，完成逐页渲染和目检，封版时以 `docs/final/11.pptx` 留档。
- [x] `DOC-12`（P0）以现有 15 页课堂汇报为母稿生成 17 页测试增强过程稿；替换 `test/v5.0/` 实测截图，修正“按建议重拍”语义，新增 65 项 JVM、7 项 Camera UI、17 个图片 ID 的测试体系页、测试集图文页和模拟器 v2/v3 中位数对比页，并通过逐页渲染、溢出检测与母稿结构一致性检查，封版时以 `docs/final/12.pptx` 留档。
- [x] `DOC-13`（P0）建立 `docs/当前功能全景与测试规划基线.md`，面向组员汇总首页、静态双方案、Native、灰度/RGB/质量/综合分析、ROI、分阶段性能和 CameraX 智能拍摄助手的全部现状；标注代码、自动化与真机验证程度，并给出下一版测试计划分类、数据集、分工和完成定义。
- [x] `DOC-14`（P0）完成 v3.0 课程封版 Git 检查点：统一 README 与交付清单中的正式 PPT 名称，纳入正式 15 页 PPT、两份过程稿和真机性能 XLSX；2026-07-10 通过 JVM、Debug APK、AndroidTest APK 与 Lint 门禁，创建并推送 `v3.0-course-final` 标签。当前无连接设备，本轮未重复 connected 测试，沿用既有 API 36.1 AVD 与 Xiaomi 14 证据并保留最终 17-ID 待填写项。

### 验收标准

- 每项结论都能追溯到代码、自动化测试、真机原始记录或截图。
- 最终文档之间的公式、阈值、版本、SDK、性能口径和操作步骤一致。
- 所有 `[待填写]` 在提交前清零，或明确标注为已知限制而不是假装完成。
- 私人照片、账号、路径、密钥和签名材料不进入公开仓库。

## 阶段 5：功能增强 v5.0（计划本地实现）

- 当前状态：**进行中（2026-07-10）**。需求来源为 `docs/功能迭代计划.md`；已完成 `v5.0-a / ENH-01~04` 的纯 Kotlin domain 能力、`v5.0-b / ENH-05~08` 的静态图片 RGB/自然语言前端接入、`v5.0-c` 的 ROI 数据结构/坐标映射/Bitmap 裁剪地基、`v5.0-d / ROI-02、05~08` 的前端框选/复用分析/恢复全图，`v5.0-e / CAM-01~05` 的 CameraX 实时预览和 Y 通道实时亮度直方图，`v5.0-f / CAM-06~07` 的智能拍摄建议，`v5.0-g / CAM-13、15` 的定格分析与保存相册，`v5.0-h / CAM-08~12、14` 的曝光补偿、补光灯和一键自动优化闭环，`v5.0-i` 的控制面板位置与定格后自动优化可用性修复，`v5.0-j` 的控制面板折叠与“按建议重拍”语义修正，以及 `v5.0-k` 的三色状态徽章和正常画面“无需重拍”交互。
- 阶段目标：在不破坏现有静态图片分析、两方案算法和 Native v3 性能链路的前提下，增强“直方图结果如何使用”的表达能力，逐步实现 RGB 三通道分析、自然语言质量判断、ROI 局部区域分析和 CameraX 智能拍摄助手。
- 负责人：`lzx`
- 实现顺序：**静态分析增强（ENH） → ROI 局部分析（ROI） → 智能拍摄助手（CAM） → 回归测试与文档同步（V5-TEST / V5-DOC）**。
- 范围边界：第一版不联网、不接真实 AI API、不上传图片、不引入数据库/后端/账号；CameraX 只做预览、实时分析、曝光补偿、补光灯和可选拍照，不做变焦、对焦、白平衡、ISO 或快门控制。

### 5.1 静态分析增强：RGB 三通道与自然语言判断

优先理由：该部分基本是纯 Kotlin domain + 小规模 UI 增强，风险低、可本地自动化测试，能直接补强“直方图有什么用”的答辩表达。

#### 开发任务与具体 TODO

- [x] `ENH-01`（P0，负责人：`lzx`；模块：`domain/color`）新增 RGB 三通道统计数据结构，包含 `redCounts/greenCounts/blueCounts`、三通道均值、主导通道、通道差异度和偏色状态；频次数组构造时复制，保证结果不可被外部修改。
- [x] `ENH-02`（P0，依赖：ENH-01；模块：`domain/color`）实现 R/G/B 三通道频次和均值统计；第一版输入为 ARGB `IntArray`，Alpha 不参与统计，阈值和现有灰度语义保持一致。
- [x] `ENH-03`（P0，依赖：ENH-02；模块：`domain/color`）实现偏红、偏绿、偏蓝、轻微偏色和色彩较均衡的规则判断；`<15` 视为均衡，`15..20` 为轻微偏色，`>=20` 为明显偏色。
- [x] `ENH-04`（P0，依赖：ENH-03、BASE-01~03；模块：`domain/insight`）实现本地自然语言图像质量判断模块，输出亮度、曝光、对比度、色彩、综合结论和建议；文案使用“可能”“建议”，不联网、不调用 AI API。
- [x] `ENH-05`（P0，依赖：ENH-01~04；模块：`ui/analyzer`）在直方图计算成功后同步生成 RGB 指标和自然语言 insight，并加入 `AnalyzerUiState`；切换方案或重新计算时会清空旧结果，失败时不影响已有成功图片。
- [x] `ENH-06`（P1，依赖：ENH-05；模块：`ui/component`）将直方图卡片改为“灰度直方图 / RGB 三通道”可切换视图，使用轻量滑动淡入动画展示 RGB 曲线、通道均值和偏色判断；不引入大型图表库。
- [x] `ENH-07`（P1，依赖：ENH-05；模块：`ui/component`）在图像质量分析卡片下方新增“综合分析”，以“结论 + 灰度依据 + 曝光依据 + 对比度依据 + RGB 偏色依据 + 建议”展示本地规则自然语言判断。
- [x] `ENH-08`（P0，依赖：ENH-01~07；模块：`test`、`docs`）补充纯红、纯绿、纯蓝、灰度图、偏暗、偏亮、低对比度、RGB 切换 UI、ViewModel 接入和 Native RGB 频次测试；已同步本 TODO 与功能迭代计划。API 36.1 模拟器全量 `connectedDebugAndroidTest` 22 项运行、1 项 benchmark 按设计跳过、0 失败。

#### 预期产物

- RGB 三通道统计模型与规则分析器。
- 本地自然语言质量判断模块。
- RGB 指标卡片和自然语言质量判断卡片。
- 对应 JVM 单元测试、Compose UI 检查和文档更新。

#### 验收标准

- 纯红/纯绿/纯蓝/灰度图的通道均值和偏色判断正确。
- 自然语言结论与已有灰度质量指标、RGB 指标一致，不联网、不调用 API。
- 原有灰度直方图、质量卡、性能卡、两方案算法和 Native v3 引擎不受影响。
- `./gradlew :app:testDebugUnitTest :app:assembleDebug :app:assembleDebugAndroidTest :app:lintDebug` 通过；有可用设备时再运行 `connectedDebugAndroidTest`。

### 5.2 ROI 区域直方图

优先理由：ROI 能体现局部直方图分析价值，但坐标映射和裁剪边界容易出错，因此放在静态增强之后实现。

#### 开发任务与具体 TODO

- [x] `ROI-01`（P0，负责人：`lzx`；模块：`domain/roi`）新增 ROI 数据结构，记录原图尺寸、ROI 坐标、宽高、像素数、面积占比和当前分析对象类型；同时提供全图/局部区域的 `AnalysisTargetInfo`。
- [x] `ROI-02`（P0，依赖：ROI-01；模块：`ui/component`）实现 ROI 选择模式 UI，在图片预览上支持拖动生成矩形框，并提供“确认区域并重新分析”“取消选择区域”；确认按钮仅在已选择算法且已画出有效矩形后启用。
- [x] `ROI-03`（P0，依赖：ROI-01；模块：`domain/roi`）实现预览坐标到 Bitmap 坐标映射，按 `ContentScale.Fit` 处理图片缩放、居中和留白；支持反向拖拽、边界裁剪、留白外拒绝和过小区域拒绝。
- [x] `ROI-04`（P0，依赖：ROI-03；模块：`data/image`）实现 ROI Bitmap 裁剪，校验 ROI 原图尺寸、过小区域和越界区域，并返回中文错误；裁剪逻辑放在 Android image 边界，不让 domain 直接依赖 Bitmap。
- [x] `ROI-05`（P0，依赖：ROI-04；模块：`ui/analyzer`）ROI 裁剪结果已复用现有分析流程：重新计算灰度直方图、RGB 指标、质量判断、自然语言 insight 和性能耗时；Native 可用时仍走 Native v3，失败时回退 Kotlin。
- [x] `ROI-06`（P1，依赖：ROI-05；模块：`ui/component`）预览卡已显示当前分析对象为“全图/局部区域”，并展示分析尺寸、分析像素和 ROI 占原图比例。
- [x] `ROI-07`（P1，依赖：ROI-05）实现“恢复全图分析”，保留原图 Bitmap、元数据和已选方案，点击后重新生成全图直方图，不丢失已选图片。
- [x] `ROI-08`（P0，依赖：ROI-01~07；模块：`test`、`docs`）补充 ROI 模型、坐标映射、留白边界、过小区域、Bitmap 裁剪像素一致性、前端拖拽确认、ROI 复用分析和恢复全图测试；使用说明与测试报告已同步。

#### 预期产物

- ROI 数据模型、坐标映射和裁剪工具。
- 图片预览上的 ROI 框选层。
- ROI 分析结果卡片与恢复全图入口。
- ROI 边界测试与使用说明。

#### 验收标准

- 用户能在图片上框选矩形 ROI，确认后显示局部直方图和质量判断。
- ROI 坐标与实际裁剪区域一致，边缘/越界/过小区域有中文提示。
- ROI 分析不破坏原图，全图分析可以恢复。
- ROI 结果与将同一区域作为独立图片分析的结果一致。

### 5.3 CameraX 智能拍摄助手

优先理由：这是展示亮点最高、但权限/生命周期/真机兼容风险最高的部分；应在 ENH 和 ROI 稳定后再本地实现。

#### 开发任务与具体 TODO

- [x] `CAM-01`（P0，负责人：`lzx`；目标文件：Version Catalog、`:app` Gradle、Manifest）增加 CameraX 稳定版 `1.6.1` 的 core/camera2/lifecycle/view 依赖和相机权限；`uses-feature android.hardware.camera` 设置为 `required=false`，不引入无关网络、数据库或 DI 框架。
- [x] `CAM-02`（P0，依赖：CAM-01；模块：`ui/home`、`ui`）首页新增“打开实时拍摄分析”入口，并扩展当前轻量 `AppDestination` 为 HOME/ANALYZER/CAMERA；未引入 Navigation Compose。
- [x] `CAM-03`（P0，依赖：CAM-01、02；模块：`ui/camera`）实现 CameraX Preview 预览页，包含返回首页、权限说明、授权入口、相机绑定加载状态和错误提示。
- [x] `CAM-04`（P0，依赖：CAM-03；模块：`data/camera`、`domain/camera`）实现 ImageAnalysis 实时帧分析，采用 `KEEP_ONLY_LATEST`，默认每约 400ms 分析一帧，并在 `finally` 中关闭 `ImageProxy`。
- [x] `CAM-05`（P0，依赖：CAM-04；模块：`ui/camera`）显示实时亮度直方图、帧尺寸、数据源、最高亮度频次、平均亮度、暗部/亮部占比、标准差和质量分类；UI 只展示最新结果。
- [x] `CAM-06`（P0，依赖：CAM-05；模块：`domain/photo`）实现 `RuleBasedPhotoCoach` 和 `PhotoCoachResult`，按严重欠曝/偏暗/轻微偏暗/严重过曝/偏亮/轻微偏亮/低对比度/正常生成中文建议；输出曝光建议和补光灯建议，但不直接控制相机。
- [x] `CAM-07`（P0，依赖：CAM-06；模块：`ui/camera`）实现智能拍摄建议卡片和本地建议生成能力，只生成建议，不修改相机参数；当前主入口已由 `CAM-15` 的预览浮层“定格分析”触发，避免用户下滑找按钮。
- [x] `CAM-08`（P0，依赖：CAM-03；模块：`domain/camera`、`data/camera`、`ui/camera`）实现曝光补偿控制：通过 `CameraInfo.exposureState` 读取支持状态、当前值和范围，通过 `CameraControl.setExposureCompensationIndex()` 调整并限制到设备支持范围内。
- [x] `CAM-09`（P0，依赖：CAM-03；模块：`domain/camera`、`data/camera`、`ui/camera`）实现补光灯控制：通过 `CameraInfo.hasFlashUnit()` 和 `torchState` 展示状态，通过 `CameraControl.enableTorch()` 开关；设备不支持时显示“无闪光灯/不支持曝光补偿”并禁用入口。
- [x] `CAM-10`（P0，依赖：CAM-06、08、09）实现“按建议重拍”：定格分析生成 `PhotoCoachResult` 后，只有建议中存在设备可执行的曝光补偿或补光灯动作时，“按建议重拍”才启用；点击后按建议调整参数并重新抓取一张新的定格画面。质量良好时显示“无需重拍”并禁用，不会在实时分析或定格时自动改相机参数，也不会把旧定格图当作已被修改。
- [x] `CAM-11`（P1，依赖：CAM-08）实现手动曝光 `+ / -`，按钮位于预览画面下方的“拍摄控制”面板，用户可连续手动微调；系统不会自动覆盖用户选择，除非用户再次点击“按建议重拍”。
- [x] `CAM-12`（P1，依赖：CAM-09）实现手动补光灯开关，按钮位于预览画面下方的“拍摄控制”面板，状态与 CameraInfo/CameraControl 同步；不支持闪光灯时禁用并给出中文提示。
- [x] `CAM-13`（P1，依赖：CAM-03）实现定格预览画面保存或拍照结果展示；当前采用 `PreviewView.bitmap` 定格画面保存到系统相册 `Pictures/ImageHistogramAnalyzer`，不引入额外相册权限。完整 `ImageCapture` 原图拍照仍保留为后续可选增强。
- [x] `CAM-14`（P0，依赖：CAM-01~13；模块：`test`、`docs`）补充相机权限、拒绝权限、无闪光灯/曝光不支持、预览异常和静态图片回归测试；同步使用说明和测试报告。Xiaomi 14 的曝光/补光灯真机数据仍归入 `V5-TEST-03` 待 lzx 复核。
- [x] `CAM-15`（P0，依赖：CAM-05、06；模块：`ui/camera`、`domain/camera`、`data/camera`）将智能分析入口前移到预览附近：用户点击实时画面或“定格分析”即可冻结当前预览图，停止显示实时变化，并基于定格图按课程 RGB 灰度公式生成直方图、质量指标和本地智能建议；下方滚动查看时数据不再被后续实时帧覆盖。2026-07-10 修正为控制按钮不覆盖画面，定格后仍保持 CameraX 绑定；后续改为默认折叠控制面板，点击“按建议重拍”时才调整参数并重新定格一张新图；预览右上角新增绿色“质量良好”、橙色“可优化”、红色“建议重拍”三色状态徽章，帮助用户先判断是否需要下滑查看原因或重拍。

#### 预期产物

- CameraX 智能拍摄助手页面。
- 实时直方图和本地规则拍摄建议。
- 曝光补偿、补光灯、按建议重拍和手动微调。
- 相机权限/设备兼容处理与真机测试记录。

#### 验收标准

- 首页可进入智能拍摄助手，权限拒绝时有清晰说明和返回入口。
- 相机预览流畅，实时直方图持续更新且不阻塞预览。
- 智能判断能在暗光/亮光/低对比度场景给出中文建议。
- 按建议重拍和手动调节均遵守设备支持范围；不支持功能时不崩溃。
- 原有静态图片分析和 ROI/ENH 功能不受影响。

### 5.4 v5.0 本地实现节奏与门禁

#### 推荐本地切片

| 切片 | 建议包含任务 | 目标 | 验证 |
|---|---|---|---|
| v5.0-a | ENH-01~04 | 纯 domain RGB/insight 能力 | 已完成；新增 12 个 JVM 单元测试 |
| v5.0-b | ENH-05~08 | 静态图片页面展示 RGB 和自然语言判断 | 已完成；JVM、assembleDebug、assembleDebugAndroidTest、lintDebug 和 API 36.1 connected 均通过 |
| v5.0-c | ROI-01、03、04、08（部分） | ROI 模型、坐标映射和裁剪正确性 | 已完成；JVM ROI 测试和 API 36.1 裁剪 instrumented 测试通过 |
| v5.0-d | ROI-02、05~08 | ROI 选择 UI 与复用分析流程 | 已完成；JVM 49 项通过，`assembleDebug`、`assembleDebugAndroidTest`、`lintDebug` 通过，API 36.1 connected 27 项运行、1 项 benchmark 跳过、0 失败 |
| v5.0-e | CAM-01~05 | CameraX 权限、预览和实时直方图 | 已完成；JVM 52 项通过，`assembleDebug`、`assembleDebugAndroidTest`、`lintDebug` 通过，API 36.1 connected 30 项运行、1 项 benchmark 跳过、0 失败；真机预览待 lzx 复核 |
| v5.0-f | CAM-06~07 | 智能拍摄建议 | 已完成；JVM 58 项通过，`assembleDebug`、`assembleDebugAndroidTest`、`lintDebug` 通过，API 36.1 AVD connected 31 项运行、1 项 benchmark 跳过、0 失败 |
| v5.0-g | CAM-13、15 | 预览浮层定格分析与保存相册 | 已完成；新增定格预览 RGB 公式直方图分析、保存相册工具和定格状态 UI；JVM 60 项通过，`assembleDebug`、`assembleDebugAndroidTest`、`lintDebug` 通过，API 36.1 AVD connected 33 项运行、1 项 benchmark 跳过、0 失败 |
| v5.0-h | CAM-08~12、14 | 曝光/补光灯手动控制与自动优化 | 已完成代码与自动化测试；新增 CameraX 调节控制器、拍摄控制面板、设备不支持状态、质量侧向集中误判修复；已通过 Kotlin 编译、JVM 单测、Debug APK、测试 APK 和 Lint，connected 因当前设备列表为空未运行，见测试报告 |
| v5.0-i | CAM-10、11、12、15 修正 | 控制按钮不遮挡预览，定格后自动优化可用 | 已完成代码与 UI 测试更新；“拍摄控制”面板移到预览画面下方，定格图覆盖显示但保留 CameraX 预览绑定和控制器 |
| v5.0-j | CAM-10、11、12、15 修正 | 控制面板默认折叠，按建议重拍 | 已完成代码、测试与文档更新；默认只保留紧凑控制栏，让实时画面和直方图更容易同屏查看；定格后按钮改为“按建议重拍”，执行相机控制后重新定格新画面；`test`、Debug APK、测试 APK、Lint 和 API 36.1 相机页 6 项 connected 测试均通过 |
| v5.0-k | CAM-10、15 体验修正 | 三色状态徽章，正常无需重拍 | 已完成代码、测试与文档更新；右上角用绿色/橙色/红色显示“质量良好/可优化/建议重拍”，定格图质量良好时按钮改为“无需重拍”并禁用，只有存在可执行相机建议时才启用“按建议重拍”；`test`、Debug APK、测试 APK、Lint 和 API 36.1 相机页 7 项 connected 测试均通过 |

#### 回归与文档 TODO

- [ ] `V5-TEST-01`（P0，依赖：ENH-08）完成静态增强功能测试：纯色图、灰度图、偏暗、偏亮、低对比度和真实图回归。
- [x] `V5-TEST-02`（P0，依赖：ROI-08）完成 ROI 功能测试：中心区域由 Compose 拖拽和 ViewModel 复用分析覆盖，边缘/留白/过小区域由坐标映射测试覆盖，恢复全图由 ViewModel 测试覆盖，手动裁剪一致性由 Bitmap 裁剪像素测试覆盖。
- [ ] `V5-TEST-03`（P0，依赖：CAM-14、CAM-15）完成智能拍摄助手真机测试：权限、预览、实时移动、右上角三色状态徽章、定格分析、保存相册、暗光/亮光建议、质量良好时“无需重拍”、手动曝光 `+/-`、补光灯开关和定格后“按建议重拍”。真机测试设备优先使用 Xiaomi 14。
- [ ] `V5-TEST-04`（P0，依赖：ENH-08、ROI-08、CAM-14）回归原有静态图片功能：选图、预览、两方案、直方图、质量卡、性能卡、300ms 口径和错误恢复。
- [ ] `V5-DOC-01`（P0，依赖：ENH-08）更新接口与数据结构说明，补充 RGB、insight 数据模型和规则阈值。
- [x] `V5-DOC-02`（P0，依赖：ROI-08）更新使用说明和测试计划，补充 ROI 操作流程、坐标边界、局部分析说明和本轮自动化验证结果。
- [x] `V5-DOC-03`（P0，依赖：CAM-14、CAM-15）已更新使用说明、测试计划、接口说明、最终需求/测试/部署文档中的 CameraX 权限、实时预览、Y 通道实时亮度直方图、定格分析、保存相册、手动曝光/补光灯和定格后按建议重拍操作。真机测试表中的实测数据继续保留占位，等待 lzx 填写。
- [x] `V5-DOC-04`（P1，依赖：V5-TEST-01~04）更新 README 效果图、最终文档和课堂 PPT，说明 v5.0 增强功能与原课程目标的关系；PPTX 和性能 XLSX 不进入本次 Git commit，17 行真机表继续保留占位。

### 本阶段风险与降级方案

| 风险 | 影响 | 降级方案 |
|---|---|---|
| RGB 统计重复读取像素 | 大图分析耗时增加 | 第一版先保证正确性，后续再评估是否扩展 Native 一次性输出 RGB；性能卡明确统计口径 |
| 自然语言判断过度绝对 | 答辩中容易被质疑 | 文案使用“可能/建议/倾向”，并保留原始数值依据 |
| ROI 坐标映射误差 | 用户看到的框与裁剪结果不一致 | 先写坐标映射单元测试，再接 UI；明确预览容器缩放和留白规则 |
| ROI 裁剪增加内存峰值 | 大图局部裁剪可能 OOM | 限制 ROI 尺寸，裁剪前校验像素数；失败时保留原图结果 |
| CameraX 兼容差异 | 某些设备无法调曝光或补光灯 | 建议功能保留，控制按钮按能力禁用；不影响静态分析 |
| 实时分析卡顿 | 相机预览不流畅 | 降低分析分辨率，延长分析间隔，使用 `KEEP_ONLY_LATEST` |
| 新功能破坏主流程 | 影响课程核心验收 | 每个切片都先跑原有静态图片回归；CameraX 与静态分析解耦 |

## 负责人总览

- 项目治理与进度：`lzx`
- 需求分析：`sjw`
- Android/Compose UI：`zdh`
- 图片读取与状态管理：`zdh`
- 直方图算法与优化：`yqj`
- 测试与性能记录：`ycx`
- 文档与答辩材料：`lzx`

## 阶段 6：公开仓库封版（2026-07-12）

- [x] `REL-01`（负责人：`lzx`）更新 GitHub README，补齐课程背景、技术栈、亮点、构建方式、测试证据和学术诚信边界。
- [x] `REL-02`（负责人：`lzx`）整理最终 PPT、需求报告、概要设计、选题报告和封版功能测试总表；移除旧 PPT 草稿。
- [x] `REL-03`（负责人：`lzx`）生成演示视频封面和说明；原始 285MB 视频仅本地保留，不纳入普通 Git 仓库。
- [x] `REL-04`（负责人：`lzx`）编写公开发布说明、小红书长短文案、视频结构、置顶评论和隐私检查清单。
- [x] `REL-05`（负责人：`lzx`）运行封版自动化门禁；单元测试、Debug APK、测试 APK 与 Lint 通过，提交、推送和公开封版标签同步完成。

预期产物：公开可读的 GitHub 仓库、最终课程材料索引、演示入口和社交平台发布文案。

验收标准：源码可构建；测试与 Lint 通过；Git 工作区无遗漏的应提交文件；大文件和本地配置未误提交；远端 `main` 与封版标签可获取。
