# 性能优化数据表

## 一、统计口径

- 核心耗时：从像素访问开始，到 256-bin 频次完成 0–100 归一化为止；图片解码和 Compose Canvas 绘制不计入。
- 开发基准：Debug/API 36 模拟器，先进行 20 次全局 JIT 预热，再对每个尺寸预热 5 次、正式运行 10 次。
- 主指标：中位数；同时保留 P90 和最差值，不能用单次最快值作结论。
- 正确性门禁：同一输入的 Kotlin v2、Native v3、两种课程方案必须逐 bin 一致，才允许比较速度。
- 模拟器只有 1 个虚拟 CPU；它适合验证 Native 直读和 C++ 热循环，但不能证明手机八核的并行加速。

## 二、模拟器版本对比表（已填写）

设备：Medium Phone AVD，Android 16/API 36，arm64-v8a，`nproc=1`。夹具为确定性不透明 ARGB_8888 Bitmap；最高频次等于像素数。

| 版本 | 做了什么 | 方案 | 分辨率/像素数 | 线程 | 核心中位数 | P90 | 最差 | 相对 v2 中位加速 |
|---|---|---|---:|---:|---:|---:|---:|---:|
| v2.0 | Kotlin 定点快路径；`getPixels` 复制到 `IntArray` | 优先灰度化 | 512×512 / 262,144 | 1 | 39.840ms | 40.908ms | 41.443ms | 基线 |
| v3.0 | Bitmap 原生直读、O3 C++、线程私有 bins | 优先灰度化 | 512×512 / 262,144 | 8 | 1.086ms | 1.377ms | 1.561ms | 36.69× |
| v2.0 | Kotlin 定点快路径；`getPixels` 复制到 `IntArray` | 统计时灰度化 | 512×512 / 262,144 | 1 | 34.245ms | 35.692ms | 38.723ms | 基线 |
| v3.0 | Bitmap 原生直读、灰度与统计融合 | 统计时灰度化 | 512×512 / 262,144 | 8 | 0.552ms | 0.591ms | 0.670ms | 62.04× |
| v2.0 | Kotlin 路径；灰度 `IntArray` 分配/GC 计入 | 优先灰度化 | 1440×1080 / 1,555,200 | 1 | 221.367ms | 224.311ms | 241.339ms | 基线 |
| v3.0 | Native 使用 1-byte 灰度缓冲，不创建 Java 彩色数组 | 优先灰度化 | 1440×1080 / 1,555,200 | 8 | 3.983ms | 4.676ms | 7.533ms | 55.58× |
| v2.0 | Kotlin 定点快路径；`getPixels` 复制到 `IntArray` | 统计时灰度化 | 1440×1080 / 1,555,200 | 1 | 8.341ms | 9.832ms | 11.953ms | 基线 |
| v3.0 | Native 直接遍历 Bitmap 并融合计数 | 统计时灰度化 | 1440×1080 / 1,555,200 | 8 | 2.035ms | 2.097ms | 2.879ms | 4.10× |
| v2.0 | Kotlin v2 完整路径 | 优先灰度化 | 3072×4096 / 12,582,912 | 1 | 95.948ms | 121.883ms | 245.425ms | 基线 |
| v3.0 | Native 直读 + 原生灰度缓冲 + 局部 bins | 优先灰度化 | 3072×4096 / 12,582,912 | 8 | 30.885ms | 32.451ms | 33.414ms | 3.11× |
| v2.0 | Kotlin v2 完整路径 | 统计时灰度化 | 3072×4096 / 12,582,912 | 1 | 64.455ms | 69.353ms | 74.055ms | 基线 |
| v3.0 | Native Bitmap 直读 + 融合计数 | 统计时灰度化 | 3072×4096 / 12,582,912 | 8 | 15.067ms | 16.305ms | 16.475ms | 4.28× |

说明：1440×1080 的 v2“优先灰度化”受 Debug ART 和 Java 大数组分配/GC 影响，出现非单调耗时；这正是 v3 移除 Java 彩色数组并将灰度缓冲压缩为 1 byte/像素的优化对象。正式课程结论仍以同一真机、同一原图、Release/接近 Release、多轮中位数为准。

## 三、模拟器线程缩放表（12.58MP，已填写）

| 方案 | Native 线程数 | 核心中位数 | P90 | 最差 |
|---|---:|---:|---:|---:|
| 优先灰度化 | 1 | 32.349ms | 37.356ms | 59.435ms |
| 优先灰度化 | 2 | 30.730ms | 31.613ms | 32.014ms |
| 优先灰度化 | 4 | 31.533ms | 47.979ms | 71.861ms |
| 优先灰度化 | 6 | 30.636ms | 31.565ms | 32.798ms |
| 优先灰度化 | 8 | 30.885ms | 32.451ms | 33.414ms |
| 统计时灰度化 | 1 | 14.715ms | 15.056ms | 22.285ms |
| 统计时灰度化 | 2 | 15.037ms | 15.153ms | 35.604ms |
| 统计时灰度化 | 4 | 14.830ms | 14.913ms | 15.078ms |
| 统计时灰度化 | 6 | 14.890ms | 15.054ms | 15.095ms |
| 统计时灰度化 | 8 | 15.067ms | 16.305ms | 16.475ms |

结论：单 vCPU 模拟器中线程数增加没有稳定收益，偶尔还会增加调度尾延迟；因此不根据此表宣称“8 线程加速”。v3 会在 Xiaomi 14 上单独比较 1/2/4/6/8 线程，最终选择中位数和 P90 都更好的线程数。

## 四、真机版本迭代表

固定设备：Xiaomi 14，第三代骁龙 8，16GB RAM，Android 16。v1/v2 来自同分辨率、同最高频次的杯子场景截图；v3 开始应固定原图并记录 SHA-256。

| 版本 | 主要改进 | 优先灰度化 | 统计时灰度化 | 相对上一版提升 | 300ms 状态 |
|---|---|---:|---:|---|---|
| v1.0 | MVP 两方案、基础分段前总计时 | 1415.321ms | 1304.234ms | 基线 | 未达到 |
| v2.0 | Kotlin 索引 `while`、分块取消、整数定点快路径 | 775.583ms | 673.735ms | 1.82× / 1.94×；下降 45.2% / 48.3% | 未达到 |
| v3.0 | NDK 28.2；`AndroidBitmap_lockPixels`；O3 C++；线程私有 256-bin；1-byte 灰度缓冲；1/2/4/6/8 线程；Kotlin 回退；界面显示引擎与线程数 | 待 lzx 真机多轮填写 | 待 lzx 真机多轮填写 | 待计算 | 待验证 |
| v3.1（条件版本） | 若 v3.0 P90 未稳定达标：按 Trace 证据尝试 ARM NEON/更低线程开销 | 待定 | 待定 | 待定 | 待定 |
| v3.2（条件版本） | 仅当包含 Bitmap/Mat 转换后仍更快，才评估 OpenCV/libyuv 等第三方库 | 待定 | 待定 | 待定 | 待定 |

真机 v3.0 每个方案先预热 5 次，再正式运行至少 10 次，填写中位数、P90、最差值；同时核对最高频次 121,520 和完整直方图一致性。不要只填一次最快结果。

## 五、优化技术分析与决策

| 方法 | 本轮是否采用 | 分析与边界 |
|---|---|---|
| 分支/定点快路径 | 已采用 | 非 `.5` 灰度直接走整数路径；仅 16,782 个精确边界回退浮点，保持课程公式语义。 |
| 多线程分块 | 已采用 | 按行分块，每线程独立 256-bin，结束后归并，避免共享计数器竞争；模拟器单核不宣传并行收益，等待八核真机数据。 |
| NDK Bitmap 直读 | 已采用 | 使用稳定的 `AndroidBitmap_lockPixels`/`unlockPixels`，取消约 48MiB Java `IntArray` 彩色像素复制。 |
| C++ 编译优化 | 已采用 | C++20、`-O3`；禁用 `fast-math`，避免灰度取整语义漂移。编译器可能自动向量化，但本项目不把它表述为“已实现显式 SIMD”。 |
| ARM NEON 显式 SIMD | 暂缓 | 灰度统计含通道拆分、取整边界分支和随机 bin 累计；先以 v3 真机 Trace 判断瓶颈。若 P90 未稳定低于 300ms，再做 NEON 原型并逐 bin 验证。 |
| OpenCV/其他第三方库 | 暂不引入 | 课程允许使用，但 Bitmap→Mat/额外格式转换、APK 体积和 ABI 维护都有成本。当前自研 Native 已消除主要复制瓶颈；第三方必须在“包含全部转换成本”后仍有明显收益才采用。 |

完整机器可读数据见 `docs/performance/模拟器性能测试_v3.csv`。
