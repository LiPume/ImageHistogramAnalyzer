# 测试计划与测试报告

## 一、测试范围

- Gradle/Compose 配置与 Launcher Activity；
- 灰度公式、取整、透明像素规则、频次统计与归一化；
- Compose 首页、整页滚动、选图按钮、方案选择、手动计算、直方图和性能状态；
- Photo Picker、图片解码、预览、元数据和完整 MVP 流程；
- Manifest、资源与 API 23 兼容静态检查。

## 二、已执行自动化测试（2026-07-06）

| 测试 | 环境 | 结果 |
|---|---|---|
| `./gradlew :app:testDebugUnitTest` | JVM / Debug | 11 项通过，0 失败 |
| `ANDROID_SERIAL=emulator-5554 ./gradlew :app:connectedDebugAndroidTest` | Medium Phone AVD，Android 16 / API 36 | 4 项通过，0 失败 |
| `./gradlew :app:assembleDebugAndroidTest` | 本机 Gradle | 通过 |
| `./gradlew :app:assembleDebug` | 本机 Gradle | 通过 |
| `./gradlew :app:lintDebug` | Android Lint | 通过；仅保留依赖可升级类警告 |

核心单元测试覆盖：

- 黑、白、红、绿、蓝灰度位置；
- RGB 公式四舍五入；
- 透明红色忽略 Alpha 后的结果；
- 频次总和与像素数量一致；
- 最高频次映射为 100；
- 2:1 比例映射为 100:50；
- 3:2:1 映射为 100:67:33；
- 空像素、错误数组长度和负频次拒绝。
- “优先灰度化”和“统计时灰度化”对混合像素生成完全一致的 256 个频次、归一化高度、像素总数和最高频次。

Compose 仪器测试覆盖：

- 空状态显示可点击选图按钮；
- 未选择方案时计算按钮禁用，选择方案后按钮可点击；
- 结果状态可滚动到直方图和性能卡片，并显示直方图语义和 300ms 达标提示。

## 三、模拟器主流程冒烟测试

- 设备：`sdk_gphone64_arm64`，Android 16 / API 36。
- 构建：Debug。
- 测试图片：PNG，1080×2400，共 2,592,000 像素。
- 结果：冷启动成功；系统 Photo Picker 成功打开；选择后只显示预览和元数据；选择方案并点击计算后显示直方图与性能卡片；页面可滚动到底部，未发现崩溃。
- 本次单次开发数据：图片解码 289.110ms，核心计算 272.955ms。

上述耗时来自 Debug 模拟器的一次运行，只能说明链路工作并在该次运行中低于 300ms，不能作为最终性能结论。

## 四、本地真实图片回归

通过 `scripts/install_test_images.sh` 将 Git 忽略的 `test_pic/` 安装到 `emulator-5554`，在 Debug/API 36 模拟器完成以下单次开发测试：

| 文件 | 尺寸 | 方案 | 最高频次 | 核心计算耗时 |
|---|---:|---|---:|---:|
| `3.jpg` | 1440×1080 | 优先灰度化 | 16,555 | 178.450ms |
| `3.jpg` | 1440×1080 | 统计时灰度化 | 16,555 | 211.232ms |
| `2.jpg` | 1620×1080 | 统计时灰度化 | 117,520 | 157.825ms |
| `1.png.jpg` | 1080×1440 | 统计时灰度化 | 520,048 | 109.494ms |

三张图片均成功完成选择、预览、方案选择、计算、滚动查看直方图和性能卡片，未发现崩溃。`3.jpg` 两种方案最高频次一致；完整 256-bin 一致性由 JVM 测试保证。以上是 Debug 模拟器单次数据，仅用于流程回归，不能作为最终性能结论。

## 五、待 lzx 验收

- [ ] 在自己的虚拟机运行并保存首页截图。
- [ ] 选择自己的 JPG/PNG，核对预览、分辨率和像素数。
- [ ] 核对直方图方向、0/128/255 标签和最高频次。
- [ ] 分别选择两个方案并点击计算，确认选图后不会自动分析且页面可滚动到底部。
- [ ] 记录核心计算耗时及是否低于 300ms。
- [ ] 连续选择至少 3 张不同图片，确认无崩溃和旧结果覆盖。
- [ ] 后续在实体设备上进行阶段 3 的 Release 多轮性能测试。

本地真实图片回归集：

| 文件 | 尺寸 | 主要覆盖场景 |
|---|---:|---|
| `test_pic/1.png.jpg` | 1080×1440 | 竖图、高反差、暗部与亮部峰值 |
| `test_pic/2.jpg` | 1620×1080 | 横图、明显噪点、宽灰度分布 |
| `test_pic/3.jpg` | 1440×1080 | 户外阴影、灰色纹理、中低灰度分布 |

执行 `ANDROID_SERIAL=<设备编号> ./scripts/install_test_images.sh` 安装到模拟器后，逐张选择并记录预览、分辨率、最高频次和核心耗时。

## 六、已知限制

- API 23–27 使用 BitmapFactory；带 EXIF 旋转信息的图片预览方向需要在基础功能阶段继续验证。
- MVP 限制单张图片不超过 1600 万像素，以降低 Bitmap 与像素数组同时存在时的 OOM 风险。
- 当前已有两种课程方案及单次耗时，尚未提供预热、多轮、中位数和加速比等正式统计学性能报告。
- 当前尚未提供现场拍照入口，已列入 `BASE-09`。
