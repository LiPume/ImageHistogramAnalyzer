# 测试计划与测试报告

## 一、测试范围

- Gradle/Compose 配置与 Launcher Activity；
- 灰度公式、取整、透明像素规则、频次统计与归一化；
- Compose 首页、首页/分析页切换、返回路径、整页滚动、选图按钮、方案选择、手动计算、直方图、质量分布和性能预算状态；
- Photo Picker、图片解码、预览、元数据和完整 MVP 流程；
- ROI 局部区域分析：预览框选、坐标映射、Bitmap 裁剪、复用直方图/RGB/质量分析流程和恢复全图；
- CameraX 实时相机分析：权限、预览、ImageAnalysis Y 通道亮度直方图、实时质量指标和返回首页；
- Manifest、资源与 API 23 兼容静态检查。

## 二、已执行自动化测试

### 2026-07-09 CameraX 实时相机 v5.0-e 增量验证

| 测试 | 环境 | 结果 |
|---|---|---|
| `./gradlew :app:testDebugUnitTest :app:assembleDebug :app:assembleDebugAndroidTest :app:lintDebug` | 本机 Gradle / Debug | JVM 52 项通过，Debug APK、测试 APK 和 Lint 均通过 |
| `./gradlew :app:connectedDebugAndroidTest` | Medium Phone AVD，Android 16 / API 36.1 | 30 项运行，1 项显式性能 benchmark 按设计跳过，0 失败 |

本轮新增或覆盖的 CameraX 测试：

- `RealtimeLumaHistogramAnalyzerTest`：覆盖紧凑 Y 平面、带 `rowStride/pixelStride` 的 Y 平面、非法尺寸/stride/数据长度拒绝，并验证直方图像素数、最高频次和质量结果；
- `HomeScreenTest.home_realtimeCameraActionOpensCameraFlow`：验证首页“打开实时拍摄分析”入口与回调；
- `CameraScreenTest.cameraWithoutPermission_showsPermissionRequestAction`：验证未授权状态显示权限说明、按钮可点击并触发授权回调；
- `CameraScreenTest.cameraWithAnalysis_showsRealtimeHistogramAndQualityMetrics`：用注入预览内容避免启动真实相机，验证实时直方图、帧尺寸、Y 通道来源和质量结论可滚动显示；
- `AndroidManifest.xml` 增加 `CAMERA` 权限和 `android.hardware.camera required=false`，保持无摄像头设备仍可安装。

真机待复核：Xiaomi 14 上首次授权、实时预览是否正常、移动镜头时直方图/质量指标是否约 400ms 更新、返回首页是否释放相机并保留静态图片分析链路。

### 2026-07-09 ROI v5.0-d 增量验证

| 测试 | 环境 | 结果 |
|---|---|---|
| `./gradlew :app:compileDebugKotlin :app:compileDebugAndroidTestKotlin` | 本机 Gradle | 通过 |
| `./gradlew :app:testDebugUnitTest` | JVM / Debug | 49 项通过，0 失败 |
| `./gradlew :app:assembleDebug` | 本机 Gradle / 四 ABI Native Debug 构建 | 通过 |
| `./gradlew :app:assembleDebugAndroidTest` | 本机 Gradle | 通过 |
| `./gradlew :app:lintDebug` | Android Lint | 通过 |
| `./gradlew :app:connectedDebugAndroidTest` | Medium Phone AVD，Android 16 / API 36.1 | 27 项运行，1 项显式性能 benchmark 按设计跳过，0 失败 |

本轮新增或覆盖的 ROI 测试：

- `PreviewRoiMapperTest`：覆盖 `ContentScale.Fit` 下无留白、水平/垂直留白、反向拖拽、仅框在留白、过小选择和非法布局；
- `RoiBitmapCropperTest`：覆盖裁剪 Bitmap 的像素一致性、ROI 原图尺寸不一致和过小 ROI 拒绝；
- `AnalyzerScreenTest.roiSelectionMode_dragConfirmSendsPreviewRectAndLayout`：在 Compose 节点上进入 ROI 模式、拖动生成矩形，并验证确认回调拿到预览矩形和图片布局；
- `AnalyzerViewModelTest.roiSelection_cropsCurrentImageRecalculatesAndRestoresFullImage`：覆盖 ROI 裁剪后复用现有分析链路生成直方图/RGB/质量数据，并可恢复原图重新分析。

仍需 lzx 真机手测：使用真实照片框选人物主体、阴影/高光边缘和很小区域，保存局部直方图与恢复全图截图；最终真机表仍以全图性能验收为主，ROI 属于增强功能。

### 历史基线（2026-07-08）

| 测试 | 环境 | 结果 |
|---|---|---|
| `./gradlew :app:testDebugUnitTest` | JVM / Debug | 23 项通过，0 失败 |
| `./gradlew :app:connectedDebugAndroidTest` | Medium Phone AVD，Android 16 / API 36.1 | 20 项通过，1 项手动性能基准按设计跳过，0 失败 |
| `./gradlew :app:assembleDebugAndroidTest` | 本机 Gradle | 通过 |
| `./gradlew :app:assembleDebug` | 本机 Gradle | 通过 |
| `./gradlew :app:lintDebug` | Android Lint | 通过；仅保留依赖可升级类警告 |
| `ANDROID_SERIAL=emulator-5554 ./scripts/run_emulator_benchmark.sh` | API 36/arm64-v8a 单 vCPU AVD | v2/v3、两方案、三尺寸、1/2/4/6/8 线程显式性能基准通过 |

核心单元测试覆盖：

- 黑、白、红、绿、蓝灰度位置；
- RGB 公式四舍五入；
- 透明红色忽略 Alpha 后的结果；
- 频次总和与像素数量一致；
- 最高频次映射为 100；
- 2:1 比例映射为 100:50；
- 3:2:1 映射为 100:67:33；
- 空像素、错误数组长度和负频次拒绝。
- “优先灰度化”和“统计时灰度化”对混合像素生成完全一致的 256 个频次、归一化高度、像素总数和最高频次。
- 可注入单调时钟能确定性记录优先灰度化的转换/计数/归一化阶段，以及统计时灰度化的融合计数/归一化阶段。
- 核心总耗时减去显式阶段后，能够得到非负的其他分配/调度开销。
- 穷举 16,777,216 种 RGB，验证整数定点快路径与优化前 `Double + roundToInt` 灰度结果逐颜色完全一致。
- 质量分析覆盖纯黑偏暗、纯白偏亮、中灰低对比度、宽灰度分布正常、暗部占比主导，以及频次总和与像素数不一致的拒绝逻辑。
- 偏暗灰度 80 和偏亮灰度 180 夹具验证平均灰度阈值分支，不依赖暗部/亮部占比兜底。

Compose 仪器测试覆盖：

- 首页显示与真实动作一致的“选择图片”按钮、三项核心能力，并正确触发选图回调；
- 已有图片时首页显示“继续查看分析”并正确触发恢复回调；
- 分析页顶部“返回首页”正确触发返回回调；
- 空状态显示可点击选图按钮；
- 未选择方案时计算按钮禁用，选择方案后按钮可点击；
- 结果状态可滚动到直方图和性能卡片，并显示直方图语义和 300ms 达标提示。
- 两种方案的性能卡片分别显示独立阶段或融合阶段，并显示取像素、归一化、其他开销和合并状态。
- Native v3 性能卡片显示执行引擎和工作线程数。
- 结果页面可滚动到质量分析卡片，正确显示质量结论与格式化指标，并能继续滚动到后续性能卡片。
- 暗部/中间调/亮部分布条与 300ms 预算条均暴露包含精确比例的可访问语义。
- 加载状态禁用重复选图，错误状态显示明确提示并保留重新选择入口，已有图片显示“重新选择图片”。
- 选择器取消不会触发图片加载或页面跳转；超大、损坏、无法打开均停止加载并显示对应中文提示；替换图片失败保留上一张成功图片和元数据。

Native 仪器测试覆盖：

- `libhistogram_native` 能在仪器设备加载；
- 黑白与 RGB 主色、已知浮点边界颜色与 Kotlin 一致；
- 穷举全部 16,782 个加权结果精确落在 `.5` 的 RGB 边界颜色，两种 Native 方案与 Kotlin 逐 bin、归一化高度完全一致；
- 混合不透明、半透明和全透明像素 Bitmap 在两种方案中与 Kotlin 完全一致；
- Native 返回 `NATIVE_V3` 引擎标识和实际工作线程数。

## 三、UI 规范迭代模拟器检查（2026-07-07）

- 设备：Medium Phone API 36.1，arm64 AVD；Debug APK。
- 首页 light mode：标题、hero、选图主按钮、三项核心能力和后续流程均可滚动查看，没有无意义空白。
- 字体缩放 1.5×：hero、功能卡正文和主按钮允许换行，未出现文字相互覆盖或操作截断。
- edge-to-edge：状态栏、TopAppBar 和底部手势区域之间没有内容遮挡。
- 自动化门禁：`testDebugUnitTest`、`assembleDebug`、`assembleDebugAndroidTest`、`lintDebug` 通过；connected tests 12 通过、1 项显式 benchmark 跳过、0 失败。
- 本轮未引入截图比对框架；视觉证据来自同一模拟器人工截图检查，课程最终验收仍需在 Xiaomi 14 复查固定 light 和大字体。

2026-07-08 根据用户选择将 B“晴空柑橘”调整为固定 light：主按钮为天空蓝，直方图/质量/性能功能模块分别使用浅蓝、薄荷绿、淡柑橘黄；方案卡片补齐蓝灰白 surface container，算法选中态改为浅蓝，不再混入 Material 默认紫灰底色或绿色选中态。质量分布使用蓝/橙/绿，性能预算使用蓝色，课程灰度直方图仍为白底黑柱。API 36.1 模拟器在系统 dark mode 下启动 App 仍为固定 light，状态栏图标保持可读；实际选图后检查方案卡未出现紫/绿冲突。`./gradlew test :app:assembleDebug :app:assembleDebugAndroidTest :app:lintDebug` 和 `:app:connectedDebugAndroidTest` 均通过；设备测试 12 项通过、1 项显式 benchmark 跳过、0 失败。

## 四、模拟器主流程冒烟测试

- 设备：`sdk_gphone64_arm64`，Android 16 / API 36。
- 构建：Debug。
- 测试图片：PNG，1080×2400，共 2,592,000 像素。
- 结果：冷启动成功；系统 Photo Picker 成功打开；选择后只显示预览和元数据；选择方案并点击计算后显示直方图与性能卡片；页面可滚动到底部，未发现崩溃。
- 本次单次开发数据：图片解码 289.110ms，核心计算 272.955ms。

上述耗时来自 Debug 模拟器的一次运行，只能说明链路工作并在该次运行中低于 300ms，不能作为最终性能结论。

`PERF-04/05` 完成后再次在同一 API 36 模拟器走通真实选图链路，选择 492×660 PNG 并执行“统计时灰度化”：Bitmap 批量取像素 4.387ms、灰度转换与频次统计融合阶段 173.654ms、归一化 0.051ms、其他分配/调度 0.218ms、核心总计 178.310ms；多线程合并正确显示为“未启用”。该单次数据只用于确认分段计时从算法到 UI 的链路生效。

`PERF-11` 单线程热循环优化后，在同一模拟器、同一 492×660 PNG、同一“统计时灰度化”方案再次单次运行：批量取像素 6.826ms、融合阶段 94.298ms、归一化 0.053ms、其他开销 0.349ms、核心总计 101.525ms，最高频次仍为 10,069。相对前一次单次核心耗时约加速 1.76 倍；由于没有预热和多轮统计，这只是开发阶段方向证据，不作为正式性能结论。

## 五、本地真实图片回归

通过 `scripts/install_test_images.sh` 将 Git 忽略的 `test_pic/` 安装到 `emulator-5554`，在 Debug/API 36 模拟器完成以下单次开发测试：

| 文件 | 尺寸 | 方案 | 最高频次 | 核心计算耗时 |
|---|---:|---|---:|---:|
| `3.jpg` | 1440×1080 | 优先灰度化 | 16,555 | 178.450ms |
| `3.jpg` | 1440×1080 | 统计时灰度化 | 16,555 | 211.232ms |
| `2.jpg` | 1620×1080 | 统计时灰度化 | 117,520 | 157.825ms |
| `1.png.jpg` | 1080×1440 | 统计时灰度化 | 520,048 | 109.494ms |

前三张图片均成功完成选择、预览、方案选择、计算、滚动查看直方图和性能卡片，未发现崩溃。`3.jpg` 两种方案最高频次一致；完整 256-bin 一致性由 JVM 测试保证。以上是 Debug 模拟器单次数据，仅用于流程回归，不能作为最终性能结论。

2026-07-06 新增 `4.png` 和 `5.png`：安装脚本已将五张图片全部推送到模拟器，MediaStore 已识别两张新增图片为 `image/png`，尺寸分别为 496×658 和 492×660。新增图片尚未记录 App 内两方案耗时，因此不在上方性能结果表中虚构数据，留待 lzx 交互验收及阶段 3 多轮测试。

## 六、首轮真机性能基线

2026-07-06，lzx 已将 App 部署到实体手机，并对同一张大图执行两个方案。截图证据如下：

| 项目 | 记录 |
|---|---|
| 图片 | `1783344131211.jpg` |
| 分辨率/像素数 | 3072×4096 / 12,582,912 |
| 最高灰度频次 | 121,520（两方案一致） |
| 图片解码 | 102.836ms |
| 优先灰度化核心计算 | 1415.321ms，未达到 300ms |
| 统计时灰度化核心计算 | 1304.234ms，未达到 300ms |
| 设备 | Xiaomi 14，第三代骁龙 8 移动平台，八核最高 3.3GHz，16GB RAM |
| 系统 | Android 16；OS `3.0.303.0.WNCCNXM.C09`；安全更新 2026-05-01 |
| 构建 | Debug/Release 类型待确认 |

这组数据说明真机大图是当前主要性能风险：较快的“统计时灰度化”仍需约 4.35 倍加速才能达到 300ms。`PERF-04/05` 随后增加了阶段边界计时和前端展示，本节提出的新版分段复测已由下一节 v2.0 记录完成。该记录是单次部署构建结果，不作为最终 Release 多轮结论。

## 七、v2.0 真机分阶段复测

2026-07-07，Xiaomi 14 使用 3072×4096、12,582,912 像素的同一杯子场景复测 `PERF-11`。本次截图文件名为 `1783403529271.jpg`；分辨率、像素数和最高频次与 v1.0 一致，但正式性能夹具仍需固定原图并记录 SHA-256。

| 阶段 | 优先灰度化 | 统计时灰度化 |
|---|---:|---:|
| 图片解码（核心外） | 113.250ms | 113.250ms |
| Bitmap 批量取像素 | 539.234ms | 499.510ms |
| 灰度转换 | 195.738ms | 与统计融合 |
| 频次统计 | 40.181ms | 173.939ms（融合） |
| 0–100 归一化 | 0.251ms | 0.225ms |
| 其他分配/调度 | 0.180ms | 0.060ms |
| 多线程结果合并 | 未启用 | 未启用 |
| 核心总耗时 | 775.583ms | 673.735ms |
| 最高灰度频次 | 121,520 | 121,520 |

| 对比项 | 优先灰度化 | 统计时灰度化 |
|---|---:|---:|
| v1.0 核心耗时 | 1415.321ms | 1304.234ms |
| v2.0 核心耗时 | 775.583ms | 673.735ms |
| 单次加速比 | 1.82× | 1.94× |
| 单次耗时下降 | 45.2% | 48.3% |
| 距离 300ms | 仍为目标的 2.59× | 仍为目标的 2.25× |

问题结论：`Bitmap.getPixels()` 占核心总时间约 69.5% / 74.1%，且单阶段已超过验收线；灰度统计多线程即使无限加速，也无法在当前像素复制路径下达到 300ms。下一轮应先完成多轮基准和像素访问实验，再决定 Kotlin 缓冲优化或 NDK `AndroidBitmap_lockPixels`。当前数据仍是单次结果，不能替代 Release 多轮中位数。

## 八、v3.0 模拟器性能回归

v3.0 使用 NDK `AndroidBitmap_lockPixels` 直读 Bitmap、O3 C++ 热循环、线程私有 256-bin 和 Native 归并；Native 不可用时保留 Kotlin v2 回退。API 36/arm64 AVD 先全局预热 20 次，每个尺寸再预热 5 次并正式采样 10 次。

12,582,912 像素夹具的核心结果：

| 方案 | Kotlin v2 中位/P90/最差 | Native v3 中位/P90/最差 | 中位加速 |
|---|---:|---:|---:|
| 优先灰度化 | 95.948 / 121.883 / 245.425ms | 30.885 / 32.451 / 33.414ms | 3.11× |
| 统计时灰度化 | 64.455 / 69.353 / 74.055ms | 15.067 / 16.305 / 16.475ms | 4.28× |

AVD 的 `nproc` 和 `/proc/cpuinfo` 均显示 1 个 CPU，因此 1/2/4/6/8 线程缩放无稳定收益；这不是 Native 多线程失败，而是当前模拟器不能提供并行硬件。完整三尺寸数据、线程矩阵和真机版本占位位于 `docs/性能优化数据表.md`，机器可读明细位于 `docs/performance/模拟器性能测试_v3.csv`。

本节是可重复的 Debug 开发基准，不替代 Xiaomi 14 的固定原图、Release/接近 Release、多轮中位数验收。

## 九、v3.0 真机单次达标记录

2026-07-07，Xiaomi 14 对 `1783408658743.jpg`（3072×4096，12,582,912 像素）运行 Native v3.0，界面显示 8 个工作线程：

| 项目 | 优先灰度化 | 统计时灰度化 |
|---|---:|---:|
| 图片解码（核心外） | 117.465ms | 117.465ms |
| 像素访问/复制 | 0.002ms | 0.003ms |
| 灰度转换 | 40.850ms | 与统计融合 |
| 频次统计 | 8.916ms | 21.264ms（融合） |
| 0–100 归一化 | 0.722ms | 0.590ms |
| 其他分配/调度 | 6.467ms | 0.385ms |
| 多线程合并 | 0.001ms | 0.002ms |
| 核心总耗时 | 56.957ms | 22.244ms |
| 最高灰度频次 | 124,455 | 124,455 |

两方案单次均明显低于 300ms，且最高频次一致。由于 v2 截图最高频次为 121,520，v2/v3 不是严格同一文件；因此约 13.62× / 30.29× 只作同尺寸相似场景参考。性能开发暂时冻结；最终验收按后续确定的 17 个 ID 单行表执行。

## 十、待 lzx 验收

- [x] 在实体手机部署并保存主流程截图。
- [x] 选择真机 JPG，核对预览、分辨率和像素数。
- [x] 核对直方图方向、0/128/255 标签和最高频次。
- [x] 分别选择两个方案并点击计算，确认选图后不会自动分析且页面可滚动到底部。
- [x] 记录首轮核心计算耗时及 300ms 结论。
- [x] 补充实体手机型号、SoC 和 Android 版本。
- [x] 使用新版性能卡片记录两方案全部分段耗时。
- [ ] 确认当前安装包是 Debug 还是 Release。
- [x] 固定 12.58MP L1 并记录 SHA-256；最终验收口径调整为每个 ID 一行、两个方案各一次。
- [ ] 连续选择至少 3 张不同图片，确认无崩溃和旧结果覆盖。
- [ ] 后续在 Xiaomi 14 上填写 17 个 ID 的完整真机验收表。
- [x] 安装 v3.0 后确认性能卡片显示“Native 多线程 v3.0”和 8 个工作线程。
- [x] Xiaomi 真机系统图片选择器可直接现场拍照，并将照片返回现有预览与分析流程；无需新增相机或相册权限。
- [ ] 在 Xiaomi 14 验收新版首页、“返回首页/继续查看分析”、质量分布条、300ms 预算条，以及固定 light/大字体可读性。
- [ ] 对固定 L1 的两个方案各运行一次，填写 `docs/performance/真机性能测试_最终模板.xlsx` 的 L1 行。
- [x] 线程调优停止：当前 App 自动请求最多 8 线程，既有真机结果余量充足，不再增加最终验收负担。

### v4.0 真机 UI 与基础功能复核（2026-07-08）

用户提供的 `test/v4.0/1.jpg` 至 `5.jpg` 显示：固定 light 的 B 方案已在 Xiaomi 14 正常呈现，首页可进入选图，分析页有返回首页路径，图片预览、方案选择、直方图、质量分布和性能预算卡片均可滚动查看。方案卡片使用蓝灰/浅蓝层级，不再出现紫色底配绿色按钮。

同一张 3072×4096、12,582,912 像素图片的两个方案最高频次均为 124,455。优先灰度化核心 38.291ms，统计时灰度化核心 23.763ms，分别占 300ms 预算 12.8% 和 7.9%；执行引擎仍为 Native 多线程 v3.0、8 个工作线程。本轮属于 UI/可靠性验收，不把单次耗时波动描述为新的性能优化。

### 确定性质量夹具与期望

| 夹具 | 256-bin 构造 | 平均灰度 | 暗部/亮部占比 | 标准差 | 期望结论 |
|---|---|---:|---:|---:|---|
| 纯黑 | bin 0 = 100 | 0 | 100% / 0% | 0 | 偏暗 |
| 纯白 | bin 255 = 100 | 255 | 0% / 100% | 0 | 偏亮 |
| 偏暗 | bin 80 = 100 | 80 | 0% / 0% | 0 | 偏暗（平均值阈值） |
| 偏亮 | bin 180 = 100 | 180 | 0% / 0% | 0 | 偏亮（平均值阈值） |
| 低对比度 | bin 128 = 20 | 128 | 0% / 0% | 0 | 低对比度 |
| 正常宽分布 | bin 64 = 50，bin 192 = 50 | 128 | 0% / 50% | 64 | 正常 |

这些夹具直接测试质量分析的确定性输入与精确输出；真实照片只用于布局、解码、直方图形态和真机流程回归，避免把主观视觉标签误当作算法真值。

本地真实图片回归集：

| 文件 | 尺寸 | 主要覆盖场景 |
|---|---:|---|
| `test_pic/1.png.jpg` | 1080×1440 | 竖图、高反差、暗部与亮部峰值 |
| `test_pic/2.jpg` | 1620×1080 | 横图、明显噪点、宽灰度分布 |
| `test_pic/3.jpg` | 1440×1080 | 户外阴影、灰色纹理、中低灰度分布 |
| `test_pic/4.png` | 496×658 | 日间彩色场景、建筑纹理、人物与中间调 |
| `test_pic/5.png` | 492×660 | 夜景彩色场景、强光源、大面积暗部与高反差 |

执行 `ANDROID_SERIAL=<设备编号> ./scripts/install_test_images.sh` 安装到模拟器后，逐张选择并记录预览、分辨率、最高频次和核心耗时。

### 最终测试图片集准备（2026-07-08）

已通过 `scripts/prepare_final_test_images.sh` 在 Git 忽略目录 `test/final/` 生成/汇总 17 张图片：11 张确定性 PNG、5 张本地真实图和 1 张 Google WebP Gallery 公开 WebP。确定性图片覆盖黑、白、灰度 80、灰度 180、低对比度、正常对比度、RGB 主色、Alpha、3072×4096 性能基线、1600 万像素边界和 16,004,000 像素超限。

`./scripts/install_test_images.sh test/final` 已在 `emulator-5554` 成功递归安装全部 17 张图片；MediaStore 抽查确认：WebP 为 1024×772、L1 为 3072×4096、上限图为 4000×4000、超限图为 4001×4000，MIME/尺寸正确。首次安装暴露 `adb` 消费循环标准输入导致仅推送首图的问题，已通过给 `adb push` 和广播重定向 `/dev/null` 修复并复测通过。

正式 L1 改为无隐私、可重复生成的 `09_performance_gradient_3072x4096.png`，SHA-256 为 `783198e25198cb7fabb18db9f690224bb4402fcb27737f46c81c1b11a2f5daec`；数学期望最高频次 75,787、平均灰度约 127.001、质量分类“正常”。图片清单、来源与全部哈希见 `docs/测试图片清单.md`。App 内两方案各一次的正式数据仍待 Xiaomi 14 填写，不把图片准备与 MediaStore 成功冒充算法验收。

## 十一、已知限制

- API 23–27 使用 BitmapFactory；带 EXIF 旋转信息的图片预览方向需要在基础功能阶段继续验证。
- MVP 限制单张图片不超过 1600 万像素，以降低 Bitmap 与像素数组同时存在时的 OOM 风险。
- 模拟器已有预热、多轮、中位数/P90/最差和加速比；真机已有单次达标证据，最终 17 行逐图验收仍待补充。
- Native 计算进行中不能被协程立即中断，但旧请求结果不会覆盖新图片。
- 静态图片现场拍照仍依赖系统 Photo Picker 是否提供相机入口；实时相机页已独立接入 CameraX，但第一版只做 Y 通道亮度直方图和质量指标，不保存照片，也不控制曝光/补光灯。
